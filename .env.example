# Path to the SQLite database file (defaults to ./data/detailflow.db)
DATABASE_PATH=./data/detailflow.db

# Secret used to sign session cookies. Change this in production!
SESSION_SECRET=change-me-to-a-long-random-string

# Public base URL, used in booking confirmation links
NEXT_PUBLIC_APP_URL=http://localhost:3000
